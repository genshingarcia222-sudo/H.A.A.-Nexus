{
  "name": "haa-nexus",
  "private": true,
  "version": "0.1.0",
  "license": "UNLICENSED",
  "description": "H.A.A. Nexus - healthcare training, simulation, assessment and competency platform",
  "scripts": {
    "test": "pnpm --filter @haa-nexus/nexus-core test",
    "typecheck": "pnpm --filter @haa-nexus/nexus-core typecheck && pnpm --filter @haa-nexus/ui-kit typecheck && pnpm --filter @haa-nexus/desktop typecheck",
    "build:core": "pnpm --filter @haa-nexus/nexus-core build",
    "dev:desktop": "pnpm --filter @haa-nexus/desktop dev",
    "build:desktop": "pnpm --filter @haa-nexus/desktop build"
  },
  "engines": {
    "node": ">=20"
  }
}
